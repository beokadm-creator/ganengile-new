/**
 * Notification Navigation Handler (Web Stub)
 * Web doesn't support expo-notifications, so this is a no-op
 */

export function handleNotificationResponse(_response: any): void {
  // No-op on web
}

export async function getInitialNotification(): Promise<void> {
  // No-op on web
}
