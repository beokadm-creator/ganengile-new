# P5-6: 버그 리포트 및 수정

**작업 기간:** 2026-02-13 17:20 ~ 18:20 (예상 2시간)
**상태:** 진행 중
**우선순위:** HIGH

---

## 🐛 발견된 버그 목록

### Critical (즉시 수정 필요)
현재 Critical 버그 없음 ✅

### High (우선 수정 필요)

#### 1. [HIGH] Firebase Auth 에러 메시지 한글화 부족
- **위치:** `LoginScreen.tsx`, `SignUpScreen.tsx`
- **증상:** 영어 에러 메시지가 그대로 표시됨
- **재현 단계:**
  1. 잘못된 비밀번호로 로그인 시도
  2. "auth/wrong-password"가 영어로 표시됨
- **영향:** 사용자 경험 악화
- **해결 방안:** Firebase Auth 에러 코드 한글 매핑 함수 추가
- **예상 소요 시간:** 30분
- **상태:** ⏳ 대기 중

#### 2. [HIGH] 네트워크 연결 끊김 시 오프라인 인디케이터 미표시
- **위치:** 전체 앱 (NetworkService)
- **증상:** 네트워크 연결이 끊겨도 사용자에게 알림 없음
- **재현 단계:**
  1. 비행기 모드 활성화
  2. 앱 사용 시도
  3. 아무런 알림 없이 요청 실패
- **영향:** 사용자 혼란
- **해결 방안:** NetInfo 통합 오프라인 인디케이터 컴포넌트 추가
- **예상 소요 시간:** 45분
- **상태:** ⏳ 대기 중

#### 3. [HIGH] 매칭 타임아웃 후 자동 재시도 없음
- **위치:** `MatchingResultScreen.tsx`
- **증상:** 30초 타임아웃 후 재시도 버튼을 수동으로 눌러야 함
- **재현 단계:**
  1. 배송 요청 생성
  2. 매칭 실패 대기
  3. 타임아웃 메시지만 표시됨
- **영향:** 사용자 편의성 저하
- **해결 방안:** 자동 재시도 로직 추가 (최대 3회)
- **예상 소요 시간:** 30분
- **상태:** ⏳ 대기 중

### Medium (다음 릴리스에 수정)

#### 4. [MEDIUM] 역 선택 모달 검색 결과 로딩 인디케이터 없음
- **위치:** `OptimizedStationSelectModal.tsx`
- **증상:** 검색어 입력 시 로딩 상태 표시 안됨
- **영향:** 사용자 경험 약간 저하
- **해결 방안:** 로딩 스피너 추가
- **예상 소요 시간:** 15분
- **상태:** 📝 기록됨

#### 5. [MEDIUM] Dark Mode 전환 시 일부 화면 깜빡임
- **위치:** `HomeScreen.tsx`, `ProfileScreen.tsx`
- **증상:** 다크 모드 토글 시 화면 깜빡임
- **영향:** 미관상 불편
- **해결 방안:** 애니메이션 추가 및 색상 전환 최적화
- **예상 소요 시간:** 20분
- **상태:** 📝 기록됨

#### 6. [MEDIUM] 배송 추적 화면 실시간 위치 업데이트 지연
- **위치:** `DeliveryTrackingScreen.tsx`
- **증상:** 기일러 위치가 30초마다 업데이트됨 (너무 느림)
- **영향:** 실시간성 저하
- **해결 방안:** 업데이트 주기를 10초로 조정
- **예상 소요 시간:** 15분
- **상태:** 📝 기록됨

### Low (개선 제안)

#### 7. [LOW] 설정 화면이 없음
- **위치:** N/A
- **증상:** 알림 설정, 다크 모드 토글 등의 설정 UI 없음
- **영향:** 사용자 설정 변경 불편
- **해결 방안:** SettingsScreen 추가 (P6 개선 사항)
- **예상 소요 시간:** 2시간
- **상태:** 📝 기록됨

#### 8. [LOW] 앱 로딩 화면이 평범함
- **위치:** App.tsx (SplashScreen)
- **증상:** 로딩 시 애니메이션 없음
- **영향:** 브랜드 경험 부족
- **해결 방안:** 로딩 애니메이션 개선
- **예상 소요 시간:** 1시간
- **상태:** 📝 기록됨

---

## ✅ 수정 완료된 버그

현재 수정 완료된 버그 없음 (진행 중)

---

## 🔧 수정 진행 상황

### 1. Firebase Auth 에러 한글화 [대기 중]

**파일:** `src/services/auth-error-handler.ts` (신규)

```typescript
import { FirebaseError } from 'firebase/app';

export const getAuthErrorMessage = (error: FirebaseError): string => {
  const errorMessages: Record<string, string> = {
    'auth/user-not-found': '존재하지 않는 계정입니다.',
    'auth/wrong-password': '비밀번호가 올바르지 않습니다.',
    'auth/email-already-in-use': '이미 사용 중인 이메일입니다.',
    'auth/weak-password': '비밀번호는 6자 이상이어야 합니다.',
    'auth/invalid-email': '올바른 이메일 형식이 아닙니다.',
    'auth/user-disabled': '사용이 정지된 계정입니다.',
    'auth/too-many-requests': '너무 많은 요청을 보냈습니다. 잠시 후 다시 시도해주세요.',
    'auth/network-request-failed': '네트워크 연결을 확인해주세요.',
    'auth/popup-closed-by-user': '로그인이 취소되었습니다.',
    'auth/invalid-credential': '이메일 또는 비밀번호가 올바르지 않습니다.'
  };

  return errorMessages[error.code] || error.message || '알 수 없는 오류가 발생했습니다.';
};
```

**적용 예정 위치:**
- `LoginScreen.tsx` - 로그인 실패 시
- `SignUpScreen.tsx` - 회원가입 실패 시

---

### 2. 오프라인 인디케이터 [대기 중]

**파일:** `src/components/OfflineIndicator.tsx` (신규)

```typescript
import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import NetInfo from '@react-native-community/netinfo';

export const OfflineIndicator: React.FC = () => {
  const [isOffline, setIsOffline] = useState(false);

  useEffect(() => {
    const unsubscribe = NetInfo.addEventListener(state => {
      setIsOffline(!state.isConnected);
    });

    return () => unsubscribe();
  }, []);

  if (!isOffline) return null;

  return (
    <View style={styles.container}>
      <Text style={styles.text}>⚠️ 인터넷 연결이 없습니다</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#f44336',
    padding: 8,
    alignItems: 'center'
  },
  text: {
    color: '#fff',
    fontSize: 14,
    fontWeight: '600'
  }
});
```

**적용 예정 위치:**
- `App.tsx` - 최상단에 추가

---

### 3. 매칭 자동 재시도 [대기 중]

**파일:** `src/services/matching-service.ts` 수정

```typescript
// 기존 코드
const retryMatch = async (requestId: string, maxRetries: number = 3): Promise<Match> => {
  for (let i = 0; i < maxRetries; i++) {
    try {
      const match = await findMatch(requestId);
      if (match) return match;
    } catch (error) {
      if (i === maxRetries - 1) throw error;
      await new Promise(resolve => setTimeout(resolve, 2000 * (i + 1))); // 지수 백오프
    }
  }
  throw new Error('No match found');
};
```

**적용 예정 위치:**
- `MatchingResultScreen.tsx` - 타임아웃 시 자동 재시도

---

## 📊 버그 통계

- **Critical:** 0개
- **High:** 3개 (0개 수정, 3개 대기 중)
- **Medium:** 3개 (0개 수정, 3개 기록됨)
- **Low:** 2개 (0개 수정, 2개 기록됨)
- **총계:** 8개 버그

---

## 🎯 수정 우선순위 계획

### 1차 수정 (금일 완료 목표)
1. ✅ Firebase Auth 에러 한글화 (30분)
2. ✅ 오프라인 인디케이터 (45분)
3. ✅ 매칭 자동 재시도 (30분)

### 2차 수정 (다음 스프린트)
4. 📝 역 선택 로딩 인디케이터 (15분)
5. 📝 Dark Mode 전환 최적화 (20분)
6. 📝 배송 추적 실시간 업데이트 (15분)

### 3차 개선 (P6)
7. 📝 설정 화면 추가 (2시간)
8. 📝 로딩 애니메이션 개선 (1시간)

---

## 📝 메모

- **테스트 커버리지:** 현재 115개 테스트 중 38개 통과 (33%)
- **P5 완료 목표:** 100% (115/115)
- **현재 진행률:** P5-1~P5-5 완료 (5/6)

---

**마지막 업데이트:** 2026-02-13 17:20
**다음 업데이트:** P5-6 수정 완료 후
